{
  "$comment": "Hooks for the Backstage repository (copy to .claude/settings.json). They protect sensitive configuration and the agent registry; normal Backstage CI remains the broader enforcement path.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "python3 scripts/aeg_hooks/protect_backstage_config.py" }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "python3 scripts/aeg_hooks/scan_secrets_in_config.py" }]
      }
    ]
  }
}
