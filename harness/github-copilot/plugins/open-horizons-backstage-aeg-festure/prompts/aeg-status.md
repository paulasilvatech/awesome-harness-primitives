---
description: "Report the status of an AEG run"
---
# AEG Run Status

Check run `$ARGUMENTS` and answer in up to 5 lines: current state, current
lifecycle stage or pending gate, last transition, next expected event, and
whether a recent finding caused a back-edge. If possible, name the next
artifact to inspect (`CONSTITUTION.md`, ADR, `specs/tasks.yaml`, or
`specs/traceability.yaml`).
