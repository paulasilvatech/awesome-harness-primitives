# Loop ledger - slice `<NNN-slug>`

One ledger per slice. Append; never rewrite a closed iteration.

## Phase: `<archaeology|vision|architecture|implementation|quality|operations>`

**Owner:** `<person or pair>` — **Budget:** 3 inner iterations — **Opened:** `<YYYY-MM-DD>`

### Iterations

| # | Change made | Check run | Actual result |
| --- | --- | --- | --- |
| 1 | `<smallest artifact produced or corrected>` | `<exact command or query>` | `<pass, fail with detail, or not-run with blocker>` |
| 2 | | | |
| 3 | | | |

### Gate evaluation

| Criterion | Result | Evidence or blocker |
| --- | --- | --- |
| `<gate id from the phase-gates reference>` | pass / fail / not-run | `<path#Lstart-Lend or command output>` |

### Classification

| Finding | Class | Owning phase | Action |
| --- | --- | --- | --- |
| `<what failed>` | local-defect / upstream-defect / accepted-deviation / environment-failure | `<phase>` | `<corrected here, DEF-NNN raised, or decision recorded>` |

### Close-out

- **Status:** closed / iterating / escalated / blocked
- **Iterations used:** `<n>/3`
- **Handoff to:** `<next phase or none>`
- **Improvement candidates:** `<pattern observed, proposed change, owner, target slice>`
- **Residual risks:** `<risk, owner, next checkpoint>`

Keep CPF values, benefit amounts, credentials, and production records out of every field.
